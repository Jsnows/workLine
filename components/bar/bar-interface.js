import state from './bar-state';
import getters from './bar-getters';
import actions from './bar-actions';
import * as bar from './bar-mutation-types';

/**
 * [mutations description]
 * @type {Object}
 *  example : [bar.SET_EXAMPLE] (state, data) { state.example = data; }
 */
const mutations = {

}

export default {
    state,
    mutations,
    getters,
    actions
}